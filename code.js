var sketchProc=function(processingInstance){ with (processingInstance){
setup = function() {
    size(600,350);
    noLoop();
    background(0, 0, 0);
  };

  draw = function() {  
    println("Hi");
  };
}};
